[project]
name = "agents-prac02"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ilyes", email = "ilyes.berrian@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "openai>=3.13.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
agents-prac02 = "agents_prac02:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[tool.uv.workspace]
members = [
    "calculator",
]
